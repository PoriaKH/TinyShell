#include <stdio.h>
#include "parse.h"

int main(int argc, char* argv[]){
   printf("hello world\n");
   //printf("getSize(argv) = %d\n",getSize(argv));
   printf("argv[0] = %s\n",argv[0]);
  // printf("argSize = %d\n",getSize(argv));
   printf("argv[1] = %s\n", argv[1]);
   return 0;
}
